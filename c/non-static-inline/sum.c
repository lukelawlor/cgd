#include "sum.h"

// This is the extern declaration for sum
// An external declaration for a non-static inline function must be present in one file in the program
extern inline int sum(int x, int y);
